<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{ post.title }}</h5>
    <p class="card-text">{{ post.description }}</p>

    <a class="btn btn-outline-dark" href="/posts" role="button">Back</a>
  </div>
</div>
